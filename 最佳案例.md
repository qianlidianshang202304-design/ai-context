# 🏆 最佳案例

> 驾驶舱角色的核心参考库。记录经过验证的最佳实践、标准答案、参考实现。
> B 级和 C 级任务开始前，驾驶舱必须先查阅此文件寻找最接近的参考案例。

---

## 使用方式

AI 在处理任务时：
1. 找到最接近的场景分类
2. 参考对应的输入输出标准
3. 复制已验证的做法，而非从零开始
4. 验收通过后，将本次的优秀做法追加到此文件

---

## 网站搭建

### 案例：内容AI工厂 — axios 统一封装 + 拦截器模式
- **场景**: Next.js 前端需要统一管理 API 请求（认证、错误处理、baseURL）
- **输入**: 各个业务模块的 API 调用需求
- **输出**: 一个 `api.ts` 文件，所有模块共享同一个 axios 实例
- **关键做法**:
  1. `axios.create()` 创建单例，配置 `baseURL` 和 `timeout`
  2. 请求拦截器自动从 localStorage 注入 Bearer token
  3. 响应拦截器统一处理 401（清 token → 跳转登录）、自动解包 `response.data`
  4. 业务 API 按模块分对象导出（`authApi`、`hotspotApi`、`analyticsApi`），每个方法声明入参类型
- **避坑**:
  - 拦截器中用 `typeof window !== "undefined"` 避免 SSR 时访问 localStorage 报错
  - `Promise.reject(error.response?.data || error)` 确保调用方拿到一致的错误格式
- **关联经验**: 见 `项目经验.md` → 内容AI工厂

### 案例：内容AI工厂 — FastAPI Router 分层 + 字段映射模式
- **场景**: FastAPI 后端需要处理 Excel 导入 → 数据库落库，列名需要做映射
- **输入**: 竞品广告 Excel 文件（Meta Ad Library 导出格式）
- **输出**: PostgreSQL `competitor_ads` 表中结构化的广告数据
- **关键做法**:
  1. 用 `COLUMN_MAP` 字典做 Excel 列名 → DB 字段名的映射（单一真相源）
  2. `APIRouter(prefix="/competitor", tags=["竞品监控"])` 做路由分组
  3. pandas 读取 Excel 后用 `rename(columns=COLUMN_MAP)` 统一字段名
  4. `AsyncSession` + `async/await` 做异步数据库操作
- **避坑**:
  - Excel 列名可能变化，映射字典要作为配置集中管理
  - 大批量导入时用 `bulk_insert_mappings` 而非逐条 insert
- **关联经验**: 见 `项目经验.md` → 内容AI工厂

---

## 内容洞察

### 案例：达人库 — TikTok Bio 批量抓取（可断点续传设计）
- **场景**: 从 TikTok 页面抓取数万条达人 Bio，需要处理反爬和请求失败
- **输入**: 包含 TikTok username 列的 Excel 文件
- **输出**: 在原 Excel 后追加 Bio、粉丝数、昵称等字段的新列
- **关键做法**:
  1. **SQLite 缓存层**：已抓取的 profile 存入 SQLite，重复运行只请求未缓存的
  2. **断点续传**：`--refresh` 标志控制是否重新抓取，默认跳过已缓存
  3. **流式读写**：用 `load_workbook(read_only=True)` 流式读 Excel，避免大文件撑爆内存
  4. **UA 轮换**：维护 USER_AGENTS 列表，每次请求随机选取
  5. **正则提取**：从 HTML 中提取 `__UNIVERSAL_DATA_FOR_REHYDRATION__` JSON 数据
  6. **并发控制**：`concurrent.futures` 控制并发数，避免被封
  7. **重试状态分类**：`RETRYABLE_STATUSES` 集合区分可重试和不可重试错误
- **避坑**:
  - TikTok 页面需要带完整的 User-Agent 和 Cookie，否则返回空
  - 正则提取 JSON 时注意处理特殊字符（`html.unescape`）
  - 请求间隔要加随机延时（`random.uniform`）
- **关联经验**: 见 `项目经验.md` → 达人库分析系统

---

## 爬虫 & 数据

### 案例：处理FB文件 — 搜索引擎间接获取社交媒体信息
- **场景**: Facebook 个人主页反爬严格，需要间接获取页面信息
- **输入**: 包含 FB profile URL 列表的 Excel 文件
- **输出**: 标注了主页分类、简介等字段的 Excel 文件
- **关键做法**:
  1. 不直接爬 FB 页面，改用 **Brave Search API** 搜索 profile URL，从搜索结果摘要中提取信息
  2. GPT API 做分类提取和归一化（将非结构化搜索结果转为结构化字段）
  3. `normalize_categories.py` 做分类名称标准化（映射不同表述到统一分类）
  4. 结果逐行追加到原 Excel，支持断点续传
- **避坑**:
  - Brave Search API 免费额度有限（2000次/月），需分批处理并做缓存
  - GPT 分类结果不稳定，要加校验层（`normalize_categories`）
  - 搜索引擎返回的结果可能不包含目标 profile，需要处理空结果
- **关联经验**: 见 `项目经验.md` → 处理FB文件

### 案例：db数据库落库 — 定时 ETL 管道模式
- **场景**: 从飞书表格读取视频链接 → Instagram API 获取数据 → 写入 PostgreSQL
- **输入**: 飞书多维表格中的视频链接列表
- **输出**: PostgreSQL 表中结构化的社交媒体验数据
- **关键做法**:
  1. 单文件脚本，`psycopg2` 直连 PostgreSQL
  2. 飞书 SDK 读取表格数据 → Instagram Graph API 获取详情 → 结构化入库
  3. 支持定时执行（cron / 手动触发）
  4. 处理 API 限流和异常重试
- **避坑**:
  - Instagram Graph API token 有过期时间，需要定期刷新
  - 飞书表格数据可能有重复，入库前做 upsert（ON CONFLICT DO UPDATE）
- **关联经验**: 见 `项目经验.md` → db数据库落库

---

## 通用实践

### 案例：项目间代码组织模式
- **场景**: Python 后端 + TypeScript 前端项目的基础结构
- **输入标准**: 
  - 后端需要 FastAPI + 异步数据库 + 路由分层
  - 前端需要 Next.js App Router + 状态管理 + API 封装
- **输出标准**:
  - 后端：`main.py` 入口 + `routers/` 路由 + `models.py` 数据模型 + `database.py` 数据库配置
  - 前端：`app/` 页面 + `components/` 组件 + `lib/api.ts` 请求封装 + `lib/store.ts` 状态管理
- **关键做法**: 
  1. `.env` 管理所有敏感配置（API key、数据库 URL）
  2. 前后端分离但共享类型定义（通过约定而非工具链）
  3. 飞书 SDK 封装为独立工具模块
- **避坑**: 
  - `.env.example` 必须提交到 Git，`.env` 必须在 `.gitignore`
  - ChromaDB 挂载路径要在 docker-compose 中明确配置

---

## 📏 输出模板

每个案例必须包含以下字段，以便驾驶舱快速对齐：

```markdown
### 案例：[一句话概括]
- **场景**: 什么情况下用
- **输入标准**: 
  - 需要什么数据/信息
  - 数据的格式要求
- **输出标准**:
  - 输出什么格式
  - 质量检查清单
- **关键做法**: 步骤或核心思路
- **避坑**: 常见错误
- **关联经验**: 见 `项目经验.md` 的哪个条目
```

---

*此文件为驾驶舱的执行参考，每次产生优秀案例后追加。最后更新: 2026-06-13*
